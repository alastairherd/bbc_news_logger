{
  "name": "bbc-news-research-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/check-built-paths.mjs",
    "check:paths": "node scripts/check-built-paths.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "astro": "^7.0.8",
    "typescript": "^5.9.3"
  }
}
